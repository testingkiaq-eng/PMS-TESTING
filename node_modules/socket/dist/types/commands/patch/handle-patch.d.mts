import constants from '../../constants.mts';
import type { OutputKind } from '../../types.mts';
export interface HandlePatchConfig {
    cwd: string;
    dryRun: boolean;
    outputKind: OutputKind;
    packages: string[];
    spinner: typeof constants.spinner;
}
export declare function handlePatch({ cwd, dryRun, outputKind, packages, spinner }: HandlePatchConfig): Promise<void>;
//# sourceMappingURL=handle-patch.d.mts.map