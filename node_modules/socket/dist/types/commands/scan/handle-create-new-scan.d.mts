import type { ReachabilityOptions } from './perform-reachability-analysis.mts';
import type { OutputKind } from '../../types.mts';
import type { Remap } from '@socketsecurity/registry/lib/objects';
export declare function handleCreateNewScan({ autoManifest, branchName, commitHash, commitMessage, committers, cwd, defaultBranch, interactive, orgSlug, outputKind, pendingHead, pullRequest, reach, readOnly, repoName, report, targets, tmp }: {
    autoManifest: boolean;
    branchName: string;
    commitHash: string;
    commitMessage: string;
    committers: string;
    cwd: string;
    defaultBranch: boolean;
    interactive: boolean;
    orgSlug: string;
    pendingHead: boolean;
    pullRequest: number;
    outputKind: OutputKind;
    reach: Remap<ReachabilityOptions & {
        runReachabilityAnalysis: boolean;
    }>;
    readOnly: boolean;
    repoName: string;
    report: boolean;
    targets: string[];
    tmp: boolean;
}): Promise<void>;
//# sourceMappingURL=handle-create-new-scan.d.mts.map